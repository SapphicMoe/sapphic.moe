---
import Link from '@components/base/Link.astro';

const { image, link, name } = Astro.props;
---

<Link href={link} title={`${name}'s site`} target="_blank">
  <img src={`webring/${image}`} alt={`Webring of ${name}`} width="88" height="31" />
</Link>
